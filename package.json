{
    "name": "shopify-orders-report",
    "version": "1.0.0",
    "scripts": {
        "build": "esbuild main.ts --bundle --outfile=dist/main.js --platform=browser"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^5.1.0",
        "node-fetch": "^2.7.0",
        "xlsx": "^0.18.5"
    },
    "devDependencies": {
        "esbuild": "^0.20.2",
        "typescript": "^5.3.3"
    }
}
